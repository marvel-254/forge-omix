'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _fetchJsonp = require('./fetch-jsonp');

var _fetchJsonp2 = _interopRequireDefault(_fetchJsonp);

var _response = require('./response');

var _response2 = _interopRequireDefault(_response);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { return step("next", value); }, function (err) { return step("throw", err); }); } } return step("next"); }); }; } /**
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          * Created by @玩家 on 16/7/6.
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          */

exports.default = function () {
    var _ref = _asyncToGenerator(regeneratorRuntime.mark(function _callee(ctx, next) {
        return regeneratorRuntime.wrap(function _callee$(_context) {
            while (1) {
                switch (_context.prev = _context.next) {
                    case 0:
                        if (!ctx.req.jsonp) {
                            _context.next = 4;
                            break;
                        }

                        return _context.abrupt('return', (0, _fetchJsonp2.default)(ctx.req.url, {
                            jsonpCallback: ctx.req.jsonpCallback,
                            body: ctx.req.body,
                            timeout: ctx.req.timeout
                        }).then(function (response) {
                            ctx._response = response;
                            var status = response.ok == true ? 200 : 404;
                            var resPromise = void 0;
                            if (status >= 200 && status < 300 || status >= 500) {
                                resPromise = response.json();
                            } else {
                                resPromise = null;
                            }
                            return resPromise;
                        }).then(function (body) {
                            var options = {
                                url: ctx.req.url,
                                status: ctx._response.ok == true ? 200 : 404,
                                statusText: ctx._response.ok == true ? 'OK' : 'Not Found',
                                ok: ctx._response.ok,
                                headers: ctx.headers,
                                body: body
                            };
                            ctx.res = new _response2.default(options);
                            return body;
                        }));

                    case 4:
                        return _context.abrupt('return', fetch(ctx.req.url, {
                            method: ctx.req.method,
                            headers: ctx.req.headers,
                            body: ctx.req.body,
                            mode: ctx.req.mode,
                            credentials: ctx.req.credentials,
                            cache: ctx.req.cache,
                            redirect: ctx.req.redirect,
                            referrer: ctx.req.referrer
                        }).then(function (response) {
                            ctx._response = response;
                            var status = response.status;
                            var type = ctx.req.type;
                            var resPromise = void 0;
                            if (status >= 200 && status < 300 || status >= 500) {
                                switch (type) {
                                    case 'arrayBuffer':
                                        resPromise = response.arrayBuffer();
                                        break;
                                    case 'json':
                                        resPromise = response.json();
                                        break;
                                    case 'text':
                                        resPromise = response.text();
                                        break;
                                    case 'blob':
                                        resPromise = response.blob();
                                        break;
                                }
                            } else {
                                resPromise = null;
                            }
                            return resPromise;
                        }).then(function (body) {
                            var options = {
                                url: ctx._response.url,
                                status: ctx._response.status,
                                statusText: ctx._response.statusText,
                                ok: ctx._response.ok,
                                type: ctx._response.type,
                                headers: ctx._response.headers,
                                body: body
                            };
                            ctx.res = new _response2.default(options);
                            return body;
                        }));

                    case 5:
                        _context.next = 7;
                        return next();

                    case 7:
                    case 'end':
                        return _context.stop();
                }
            }
        }, _callee, this);
    }));

    return function (_x, _x2) {
        return _ref.apply(this, arguments);
    };
}();

module.exports = exports['default'];