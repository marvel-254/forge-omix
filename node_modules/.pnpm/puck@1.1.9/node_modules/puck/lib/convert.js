'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = convert;

var _co = require('co');

var _co2 = _interopRequireDefault(_co);

var _compose = require('./compose');

var _compose2 = _interopRequireDefault(_compose);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var _marked = [createGenerator].map(regeneratorRuntime.mark); /**
                                                               * Created by @玩家 on 16/7/14.
                                                               */

function convert(mw) {
    if (typeof mw !== 'function') {
        throw new TypeError('middleware must be a function');
    }
    if (mw.constructor.name !== 'GeneratorFunction') {
        // assume it's Promise-based middleware
        return mw;
    }
    var converted = function converted(ctx, next) {
        return _co2.default.call(ctx, mw.call(ctx, createGenerator(next)));
    };
    converted._name = mw._name || mw.name;
    return converted;
}

function createGenerator(next) {
    return regeneratorRuntime.wrap(function createGenerator$(_context) {
        while (1) {
            switch (_context.prev = _context.next) {
                case 0:
                    _context.next = 2;
                    return next();

                case 2:
                    return _context.abrupt('return', _context.sent);

                case 3:
                case 'end':
                    return _context.stop();
            }
        }
    }, _marked[0], this);
}

// convert.compose(mw, mw, mw)
// convert.compose([mw, mw, mw])
convert.compose = function (arr) {
    if (!Array.isArray(arr)) {
        arr = Array.from(arguments);
    }
    return (0, _compose2.default)(arr.map(convert));
};

convert.back = function (mw) {
    if (typeof mw !== 'function') {
        throw new TypeError('middleware must be a function');
    }
    if (mw.constructor.name === 'GeneratorFunction') {
        // assume it's generator middleware
        return mw;
    }
    var converted = regeneratorRuntime.mark(function converted(next) {
        var ctx, called;
        return regeneratorRuntime.wrap(function converted$(_context2) {
            while (1) {
                switch (_context2.prev = _context2.next) {
                    case 0:
                        ctx = this;
                        called = false;
                        // no need try...catch here, it's ok even `mw()` throw exception

                        _context2.next = 4;
                        return Promise.resolve(mw(ctx, function () {
                            if (called) {
                                // guard against multiple next() calls
                                // https://github.com/koajs/compose/blob/4e3e96baf58b817d71bd44a8c0d78bb42623aa95/index.js#L36
                                return Promise.reject(new Error('next() called multiple times'));
                            }
                            called = true;
                            return _co2.default.call(ctx, next);
                        }));

                    case 4:
                    case 'end':
                        return _context2.stop();
                }
            }
        }, converted, this);
    });
    converted._name = mw._name || mw.name;
    return converted;
};
module.exports = exports['default'];