services:
  forge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - forge-data:/data
    environment:
      - PORT=3001
    restart: unless-stopped

volumes:
  forge-data:
